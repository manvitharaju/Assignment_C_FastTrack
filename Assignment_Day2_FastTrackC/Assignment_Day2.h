
//Enter a 5 digit no. and to find its sum
int sum_of_five_digits_modulus_using_for(int input);
// to reverse a number
int reverse_number(int n);
// to find the count of a digit in a number.
int count_digit(int num,int digit);
//to check if no. is prime or not
int is_prime_number(int pnum);
//Generate N prime numbers
int generate_first_n_prime_numbers(int limit);
// to check if a no. is palindrome or not
int palindrome(int);
// to display and find sum of 1+11+111+111 till N
int one_series(int n);
// to check if a no. is armstrong or not
int isArmstrong(int number);
//amicable number or not
int amicable(int num1,int num2);
//menu driven program to find sum,difference,product of 2 nos.
void menu_add_sub_mul();
